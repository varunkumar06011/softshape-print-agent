{
  "name": "softshape-print-agent",
  "version": "0.1.0",
  "description": "Windows Print Agent for SoftShape — silent ESC/POS printing via Socket.IO",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "tauri": "tauri",
    "test": "vitest run"
  },
  "dependencies": {
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.0",
    "vite": "^5.3.0",
    "vitest": "^1.6.0"
  }
}
